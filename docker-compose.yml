# Defines local development services for the FitFlow backend and AI engine.
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
  ai-service:
    build: ./ai-service
    ports:
      - "8000:8000"
